rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: Open access for development. 
      // Allows anyone to read/write.
      allow read, write: if true;
    }
  }
}
